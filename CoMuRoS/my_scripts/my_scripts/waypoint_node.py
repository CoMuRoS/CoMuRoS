from send_navigation_goal_node import send_navigation_goal

if __name__ == "__main__":
    # Send multiple navigation goals
    send_navigation_goal(0.0, 0.0, 0)
    # send_navigation_goal(2.0, 0.0, 90)
    # send_navigation_goal(2.0, 0.0, 45)
